FROM node:20-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./
COPY tsconfig.json ./

# Install ALL dependencies
RUN npm install

# Copy worker code
COPY . .

# Run TypeScript directly with tsx for hot reload
CMD ["npx", "tsx", "watch", "index.ts"]
